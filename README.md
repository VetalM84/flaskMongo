## MongoDB playground using Flask and pyMongo

What is implemented here:

- all CRUD methods with filtering and projection,
- aggregation with $group and $lookup,
- transactions,
- one-to-many relationship,
- validation schema.